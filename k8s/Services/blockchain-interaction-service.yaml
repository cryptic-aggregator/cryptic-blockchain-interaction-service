apiVersion: v1
kind: Service
metadata:
  name: blockchain-interaction-service
spec:
  selector:
    app: blockchain-interaction
  ports:
    - protocol: TCP
      port: 4001
      targetPort: 4001
  type: ClusterIP